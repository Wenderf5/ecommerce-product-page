const classproduto = class produtocarrinho{
    constructor(id, titulo, desc, preco, desconto, quantidade){
        this.id = id,
        this.titulo = titulo,
        this.desc = desc,
        this.preco = preco,
        this.desconto = desconto,
        this.quantidade = quantidade
    }
}
export default classproduto